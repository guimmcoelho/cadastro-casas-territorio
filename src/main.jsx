import React from "react";
import ReactDOM from "react-dom/client";
import CadastroTerritorio from "./App";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CadastroTerritorio />
  </React.StrictMode>
);
